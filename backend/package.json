{
  "name": "email-tracker-backend",
  "version": "1.0.0",
  "description": "Tracking server for Gmail Email Tracker extension",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "setup-db": "node -e \"require('./db').runSchema()\"",
    "generate-icons": "node generate-icons.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  }
}
